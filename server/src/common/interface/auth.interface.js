"use strict";

// Interface definitions are removed in JavaScript
// These were TypeScript interfaces used for type checking

// RegisterDto fields: name, email, password, confirmPassword, userAgent (optional)
// LoginDto fields: email, password, userAgent (optional)
// resetPasswordDto fields: password, verificationCode

module.exports = {};
