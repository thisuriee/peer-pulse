"use strict";

const passport = require("passport");
//const { setupJwtStrategy } = require("../../common/strategies/jwt.strategy");

const intializePassport = () => {
  //setupJwtStrategy(passport);
};

intializePassport();
module.exports = passport;
